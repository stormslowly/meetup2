$(function() {
	$('#inputBeginDate').pickmeup({
		position: 'right',
		hide_on_select: true,
		format: 'Y-m-d',
	});

	$('#inputEndDate').pickmeup({
		position: 'right',
		hide_on_select: true,
		format: 'Y-m-d',
	});

});